# 《回响之刃》音频设计说明

## 1. 文档用途

本文档说明 `sounds/` 内背景音乐与音效的听感定位、技术规格和触发方式，作为后续增删音频素材的统一依据。

相关文件：

- 游戏总体设计：`game_design.md`
- 图片资源规范：`images_design.md`
- 素材生成脚本：`generate_audio_assets.py`
- 运行时音频系统：`game/audio.py`
- 音频资源目录：`sounds/`

## 2. 听感定位

整体延续“夜色灰塔 + 被复制记忆侵蚀的都市 + 刀锋回响”的主题，音色偏冷、留白多，金属音效承担关键反馈。

| 场景 | 定位 | 手法 |
| --- | --- | --- |
| 大厅 | 舒缓、克制、可长时间停留 | A 小调五声音阶、慢速铺底、分解和弦与回声延迟 |
| 战斗 | 有推进感但不轰头 | 太鼓与踩镲建立节奏，八分音符低音 ostinato，中高频为主旋律 |
| 音效 | 轻、短、干脆 | 短衰减包络、避开低频浑浊区、同帧限流 |

刀剑音效不追求写实采样，而是强调“可分辨的确认感”：不同事件在**音高重心**和**包络长度**上明显区分，玩家不必看画面也能判断发生了什么。

## 3. 混音规则（战斗音乐不盖住刀音）

| 规则 | 数值 | 目的 |
| --- | --- | --- |
| 音乐总线 | 用户音量的 55% | 音乐整体让位 |
| 音效总线 | 用户音量的 88% | 音效保持前景，同时为挥刀+命中的同帧叠加留出余量 |
| 音乐挖坑 | 3kHz 处 -5dB、4.2kHz 处 -2dB | 让出挥刀/弹刀的“确认感频段” |
| 音乐低通 | 8.2kHz 二级低通 | 高频清晰度留给刀锋与火花 |
| 音乐高通 | 36Hz | 去掉无用超低频，避免浑浊 |
| 闪避（ducking） | 弹刀 -50%/0.45s、受击 -34%/0.3s、命中 -22%/0.18s | 关键音效出现时音乐短暂后退 |
| 弹刀电平 | -1.5 dBFS | 全套素材中最响，形成最强确认感 |

大厅音乐不挖坑，仅做温和的低通与降采样，保持温暖听感。

## 4. 背景音乐

| 文件 | 时长 | 规格 | 调性 / 速度 | 循环 | 峰值 |
| --- | ---: | --- | --- | --- | ---: |
| `sounds/music/lobby_theme.wav` | 61.94s | 22050Hz 立体声 | A 小调 / 62 BPM / 16 小节 | 无缝 | -7.0dB |
| `sounds/music/battle_theme.wav` | 54.86s | 22050Hz 立体声 | A 小调 / 140 BPM / 32 小节 | 正拍对齐 | -6.5dB |

### 4.1 大厅主题：灰塔回响

和声走向为 `Am7 – Fmaj7 – Cmaj7 – G6`，第 5～8 小节换成 `Am7 – Fmaj7 – Dm7 – G6`，第 9～12 小节转入 `Fmaj7 – G – Em7 – Am` 的对位段，末 4 小节回到主进行并以 `G7` 导向循环点。

分层进入顺序：

1. 铺底弦垫（三个微失谐振荡器，慢起音）
2. 低音长音
3. 分解和弦（前 4 小节稀疏，之后逐段加密）
4. 气声主旋律（第 5 小节进入，五声音阶，分句留白）
5. 中低音对位线与高频空气感点缀（后半段）
6. 回响钟声点缀（第 5、9、13、16 小节）

抗重复手段：4 小节为一段，纹理逐段增减；分解和弦方向逐小节交替；每小节力度与微时值抖动；主旋律使用不同分句；回声延迟跨小节环绕。

### 4.2 战斗主题：折光长刃

8 小节为一段，共 4 段：

| 段落 | 小节 | 内容 |
| --- | --- | --- |
| 主段 | 1–8 | 太鼓 + 边击 + 踩镲 + 八分低音 ostinato |
| 加旋律 | 9–16 | 气声主旋律进入，鼓组填充加密 |
| 蓄势段 | 17–24 | 17–20 小节半场抽稀，21–24 小节加花推进 |
| 张力段 | 25–32 | 属和弦（E）制造未解决感，末小节滚奏导向循环点 |

踩镲保持在 6kHz 以上只提供推进感，节奏重音交给 90–170Hz 的太鼓，避免与刀音争夺中频。

### 4.3 循环处理

两首音乐都按整数小节生成。长音符、铺底与回声在循环点处**环绕衔接**：生成时先渲染一个“上一轮结尾”的预滚区，再裁掉预滚，使上一轮的尾音自然延续到本轮开头。

实测接缝跳变：大厅 0.0018、战斗 0.0252，均低于曲内最大相邻采样跳变（0.058 / 0.082），且战斗主题在循环点是太鼓重音，听感上不会出现咔哒声。

## 5. 音效

| 逻辑名 | 文件 | 时长 | 峰值 | 设计要点 |
| --- | --- | ---: | ---: | --- |
| 脚步 ×4 | `sfx/step_0..3.wav` | 0.11s | -20.0dB | 低通短噪声 + 极轻闷响，4 个变体随机播放 |
| 落地 | `sfx/land.wav` | 0.24s | -13.0dB | 60Hz 闷响 + 快速下沉噪声 |
| 起跳 | `sfx/jump.wav` | 0.28s | -16.0dB | 上扬气声 + 250→570Hz 滑音，刻意不沉重 |
| 冲刺 / 闪避 | `sfx/dash.wav` | 0.30s | -15.0dB | 带通噪声双向扫频，无低频撞击 |
| 挥刀 ×3 | `sfx/swing_1..3.wav` | 0.40s | -7.5 / -7.0 / -6.5dB | 宽频气流 + 中低频刀身 + 刃鸣；三段音高 ×1.0 / ×1.41 / ×2.0 |
| 命中敌人 | `sfx/hit.wav` | 0.34s | -4.5dB | 4ms 硬瞬态 + 中频爆裂 + 极短金属撞击 + 紧实冲击 |
| 敌人起手 | `sfx/enemy_attack.wav` | 0.36s | -14.0dB | 比玩家挥刀更暗更低的气声 + 低频脉动 |
| 角色受击 | `sfx/hurt.wav` | 0.42s | -10.5dB | 低频闷响 + 短促失真 |
| 弹刀架势 | `sfx/parry_ready.wav` | 0.22s | -16.0dB | 极短金属轻响，提示弹刀窗口 |
| **弹刀成功** | `sfx/parry.wav` | 0.42s | **-1.5dB** | 短促金属撞击：2450Hz 主金属体 + 760Hz 躯干 + 220Hz 冲击体 + 32ms 单次拍击 |
| 敌人登场 | `sfx/spawn.wav` | 0.90s | -13.0dB | 低位膨胀噪声「接近」+ 落地冲击 + 金属余韵 |
| 传送门开启 | `sfx/portal_open.wav` | 1.10s | -11.0dB | 上升膨胀噪声 + 明亮钟体，像门被推开 |
| 进入传送门 | `sfx/portal_enter.wav` | 1.00s | -10.0dB | 气流下坠 + 明亮钟体 + 低频收束 |
| 击败敌人 | `sfx/defeat.wav` | 0.70s | -12.0dB | 回响碎裂：下沉噪声 + 钟体 + 低频收束 |

挥刀、命中与弹刀在 2026-09 做过多轮重做。最终版本不再靠“堆层数 + 猜音量”，而是先用
分倍频带通测量**真实的频带能量分布**，再显式塑形频谱：

- **挥刀**：白噪声天然是每升高一个八度能量更多，所以纯噪声做出来的挥刀必然是嘶声。
  现在先铺宽频气流（低通扫频）与中低频刀身，再用 350Hz 低架 +6dB、900Hz 峰值 +5dB、
  4kHz 高架 -9dB 把能量压回中频，最后软削波增加密度。实测 500Hz~2kHz 形成平坦能量带，
  频谱重心从 1624Hz 逐段升到 1926Hz、2316Hz。
- **三段连击音高**：整体倍数 1.0 / 1.41 / 2.0（+6 / +12 半音），并额外加入一段随音高缩放的
  音调下扫作为音高线索，让三连击“一次比一次高”能够一耳听出。
- **命中敌人**：偏软的原因是缺少 500~1500Hz 的中频层（那一带原本根本没有声源），
  只剩低频轰响加高频脆响。现在补上中频爆裂层，并把 900Hz 抬 +4dB、5kHz 压 -6dB，
  所有层的衰减都在 80ms 以内，整体是一个干脆的撞击脉冲。
- **弹刀**：曾经因为主钟体衰减 0.9 秒、又叠了一串 0.19 秒延迟回声，听感变成“敲钟”。
  现在改成**短促金属撞击**：2450Hz 主金属体 + 2480Hz 微失谐层（各分音衰减 0.02~0.095 秒）
  + 760Hz 中频躯干 + 220Hz 低频冲击体 + 5200Hz 高频咬合层 + 12ms 硬起音，长回声整串去掉。
  为了在“干脆”的前提下加强力度，又补了两项密度手段：**32ms 单次拍击**（无反馈，听感上
  是同一击的一部分，不会拖尾）与**软削波 drive 2.2**（提高有效音量而不抬高峰值）。
  峰值提到 -1.5dB，有效值 -14.3dB——比命中音（-19.0dB）高 4.7dB，是全套最响的素材。
  频谱重心约 2.85kHz（主金属体 2450Hz），能量集中在 2~4kHz，350ms 内衰减 28dB，仍然干脆。

### 5.1 确认感的分工

- **发动攻击就有声**：`swing_*` 不含金属芯，只表示“刀已挥出”。
- **命中敌人才有的额外反馈**：`hit` 加入金属双钟与高频碎响，与挥刀音区分明显。
- **敌人起手就有声**：`enemy_attack` 用更暗、更低的音色，与玩家挥刀区分，便于靠听辨预判。
- **造成伤害的额外反馈**：`hurt` 用闷响表示“真的挨打了”。
- **弹刀**：单一声高亮金属撞击，峰值最高（-3.0dB），且收得干脆——即使战斗激烈也不会糊住后续动作。

音效整体电平次序（峰值）：弹刀 -1.5dB > 命中 -4.5dB > 挥刀 -6.5~-7.5dB > 进门 -10.0dB >
受击 -10.5dB > 开门 -11.0dB > 击败 -12.0dB > 登场 -13.0dB > 落地 -13.0dB > 敌人起手 -14.0dB > 冲刺 -15.0dB > 起跳 -16.0dB >
架势 -16.0dB > 脚步 -20.0dB。

### 5.2 调节旋钮

需要微调时可以按下面顺序改（都在 `generate_audio_assets.py`）：

| 想要的效果 | 调整方式 |
| --- | --- |
| 更厚、更沉 | 提高 `low_shelf_eq` 与 `peaking_eq` 的中低频增益（挥刀 350Hz +6 / 900Hz +5） |
| 太闷、想通透一点 | 降低上述增益，或把 `high_shelf_eq` 从 -9dB 放宽到 -5dB |
| 太脆、太尖 | 降低刃鸣 `add_bell` 的增益，或把 `high_shelf_eq` 频率从 4kHz 降到 3kHz |
| 更硬、更有冲击 | 提高 `add_kick` 的 `click` 与 `decay`，并加大 `soft_clip` 的 drive |
| 连击音高差不够明显 | 改 `build_swing_sfx` 里的 `pitch = (1.0, 1.41, 2.0)` |
| 弹刀更高或更低 | 改 `build_parry_sfx` 里各层金属体的基频（760 / 2450 / 2480 / 5200Hz） |
| 弹刀想更拖一点或更利落 | 改 `add_bell` 的 `taus`（当前 0.02~0.095 秒），或调整 `Buffer(0.4)` 的总长度 |
| 弹刀还要更强 | 提高 220Hz 冲击体与 `low_shelf_eq` 的增益，或加大 `soft_clip` 的 drive |
| 弹刀想更亮更尖 | 提高 2450Hz 主金属体与 5200Hz 咬合层的增益，降低 `low_shelf_eq` |

### 5.3 怎么客观检查音色

单频点探针会被钟体的离散分音误导（分音落在探针之间就测不到）。判断音色请用**分倍频带通测能量**：
把信号分别通过 125 / 250 / 500 / 1k / 2k / 4k / 8k 的二阶带通，比较各频带能量占总能量的比例，
再算能量加权重心。健康的形态大致是：

| 音效 | 期望形态 |
| --- | --- |
| 挥刀 | 500Hz~2kHz 平坦，重心 1.5~2.5kHz，并随连击段数上移 |
| 命中 | 250Hz~4kHz 较平坦，重心 2kHz 左右 |
| 弹刀 | 峰值在 2kHz 左右，250ms 内衰减 20dB 以上（不拖尾） |
| 受击 | 峰值在 125~250Hz，向高频单调衰减 |

## 6. 触发映射

| 游戏事件 | 音频行为 |
| --- | --- |
| 主菜单 / 大厅 / 结算 / 失败界面 | 播放 `lobby_theme` |
| 进入关卡（含新手教学关卡） | 交叉淡化到 `battle_theme` |
| 地面跑动 | 按实际速度累积节拍播放 `step_*` |
| 落地 / 起跳 / 冲刺 | `land` / `jump` / `dash` |
| 玩家发动攻击（含未命中） | `swing_1..3`（按连击段数） |
| 命中敌人 | `hit`，并压低音乐 |
| 敌人发动攻击 | `enemy_attack` |
| 完美弹刀 | `parry`，音乐压低 50% |
| 关卡内敌人登场（延迟 3 秒后） | `spawn` |
| 清空房间、回响之门开启 | `portal_open` |
| 走进传送门触发进入动画 | `portal_enter` |
| 角色被命中造成伤害 | `hurt`，并压低音乐 |
| 击败敌人 | `defeat` |
| 设置里的音量滑杆 | 同时作用于音乐与音效两条总线 |

## 7. 重新生成与试听

```powershell
# 重新生成全部素材
.\.venv\Scripts\python.exe generate_audio_assets.py

# 只生成音乐或只生成音效
.\.venv\Scripts\python.exe generate_audio_assets.py --only music
.\.venv\Scripts\python.exe generate_audio_assets.py --only sfx

# 生成后立刻试听（需要可用的音频设备）
.\.venv\Scripts\python.exe generate_audio_assets.py --preview
```

生成脚本只依赖 Python 标准库（`math` / `random` / `array` / `wave`），运行后会输出每个文件的时长、采样率、峰值与有效值，便于核对电平。

## 8. 可调参数

| 想调整的内容 | 位置 |
| --- | --- |
| 大厅速度、小节数、和声进行、旋律 | `generate_audio_assets.py` 中 `LOBBY_BPM` / `LOBBY_BARS` / `LOBBY_PROGRESSION` / `LOBBY_MELODY` |
| 战斗速度、小节数、和声与旋律 | `BATTLE_BPM` / `BATTLE_BARS` / `BATTLE_PROGRESSION` / `BATTLE_LEAD` |
| 音乐让出的频段 | `build_battle_theme()` 末尾的 `peaking_eq` 与 `lowpass` |
| 音乐与音效比例 | `game/audio.py` 的 `MUSIC_BUS` / `SFX_BUS` |
| 单个音效的相对音量 | `game/audio.py` 的 `SFX_GAIN` |
| 同帧音效限流 | `game/audio.py` 的 `SFX_MIN_INTERVAL` |
| 音乐压低幅度与时长 | 调用处的 `audio.duck(amount, duration)` |

## 9. 当前限制与后续可做

- 素材为程序化合成版本，可作为原型直接使用，后续可替换为录制或采样版本而不改动调用方式。
- 音乐为 WAV（约 4.7～5.3MB/首），发布阶段可转 OGG 压缩，但需验证循环点是否被编码器延迟破坏。
- 尚未接入：首领阶段的多层音乐切换、环境音、UI 音效、弹刀失败（过早）专用音效、音量分轨（BGM / 音效分别调节）。
