# 《回响之刃》开始界面图片设计说明

## 1. 文档用途

本文档说明 `images/` 文件夹内开始界面图片的视觉定位、技术规格和推荐用法，为后续使用 Python/Pygame 开发菜单场景提供统一依据。

相关文件：

- 游戏总体设计：`game_design.md`
- 图片生成脚本：`generate_start_assets.ps1`
- 图片资源目录：`images/`

## 2. 整体视觉规范

开始界面延续《回响之刃》的 2D 像素 Roguelike 风格，以夜色中的失落都市“灰塔”为背景，通过青绿色回响能量和少量红色警示色突出弹刀主题。

### 色彩用途

| 色彩 | 推荐用途 |
| --- | --- |
| 深蓝黑 | 夜空、建筑剪影、菜单底色 |
| 青绿色 | 回响能量、选中状态、弹刀反馈 |
| 淡青白 | 刀光、高亮边框、关键提示 |
| 珊瑚红 | 危险、核心能量、少量视觉强调 |

所有图片采用清晰的硬边像素，不应使用双线性过滤。游戏缩放图片时应使用整数倍缩放或 `pygame.transform.scale()`，避免使用平滑缩放导致像素边缘模糊。

## 3. 资源清单

| 文件名 | 尺寸 | 透明背景 | 主要用途 |
| --- | ---: | :---: | --- |
| `title_background.png` | 1280×720 | 否 | 开始界面全屏背景 |
| `logo_emblem.png` | 256×256 | 是 | 游戏标题旁的回响之刃标志 |
| `player_idle.png` | 96×120 | 是 | 开始界面角色展示或存档槽头像 |
| `menu_panel.png` | 640×128 | 是 | 菜单按钮的背景面板 |
| `menu_cursor.png` | 48×48 | 是 | 当前菜单选项的指示光标 |
| `parry_spark.png` | 128×128 | 是 | 弹刀主题的闪光动画素材 |

## 4. 单项图片说明

### 4.1 `title_background.png`

开始界面的主背景图。画面包含夜空、残月、远近两层建筑剪影、中央发光城门和地面回响碎片。

- 原始尺寸即目标窗口尺寸，可直接用于 1280×720 窗口。
- 中央城门承担视觉焦点，标题建议放在屏幕上方，菜单放在中央偏下区域。
- 背景不包含文字，便于后续调整游戏名、语言和菜单内容。
- 若窗口比例不是 16:9，应优先等比缩放并裁切两侧，不建议直接拉伸。

推荐绘制位置：`(0, 0)`。

### 4.2 `logo_emblem.png`

透明背景的回响之刃徽记。外形由结晶、刀刃和红色能量核心组成，用于强化游戏识别度。

- 推荐放在游戏标题左侧或标题正上方。
- 建议显示尺寸为 128×128 至 192×192。
- 可以缓慢上下浮动 2～4 像素，或周期性改变透明度，但不要进行连续旋转。
- 标题文字应由游戏字体实时绘制，不应写入徽记图片。

### 4.3 `player_idle.png`

透明背景的玩家待机形象，表现持有回响长刃的主角。

- 推荐放置在中央城门前方，使角色和场景产生空间联系。
- 可作为静态展示，也可在后续加入多个待机帧替换当前单帧。
- 建议保持原尺寸或按整数倍缩放，不要进行非等比拉伸。
- 角色脚部应与背景地面线对齐，避免悬空。

1280×720 界面的推荐位置：角色左上角约为 `(592, 510)`，实际位置可根据标题和菜单调整。

### 4.4 `menu_panel.png`

透明背景的像素菜单面板，用于承载“开始游戏”“继续游戏”“排行榜”“设置”“退出游戏”等菜单文字。

- 原始尺寸为 640×128，适合作为横向主菜单按钮。
- 同一页面的按钮应使用统一尺寸和间距。
- 默认状态可使用 70%～85% 不透明度；选中状态使用完整不透明度。
- 菜单文字应实时居中绘制，避免把文字固定在图片中。
- 不建议在面板内部再嵌套其他卡片或边框。

推荐按钮显示尺寸：`320×64` 或 `400×80`。按钮之间保留 12～20 像素垂直间距。

### 4.5 `menu_cursor.png`

透明背景的刀刃形菜单光标，用于标识键盘或手柄当前选中的菜单项。

- 默认放在选中面板左侧，并与面板垂直居中。
- 切换选项时可使用 0.08～0.12 秒位移动画。
- 可在 80%～100% 透明度之间呼吸变化，提示当前焦点。
- 鼠标悬停和键盘选中应共享同一选中状态，避免出现两个光标。

### 4.6 `parry_spark.png`

透明背景的弹刀闪光素材，用于开始界面的环境动画，也可在战斗原型中作为临时弹刀特效。

- 在开始界面中可每隔 2～4 秒于徽记或刀刃附近短暂播放。
- 推荐动画时长为 0.12～0.20 秒，按“快速放大、快速消失”的节奏呈现。
- 不应持续常亮，否则会削弱弹刀成功时的奖励感。
- 后续战斗版本应扩展为 4～6 帧动画，并配合短暂停顿和金属音效。

## 5. 推荐界面构图

以 1280×720 为基准，推荐使用以下图层顺序：

1. `title_background.png`：铺满窗口。
2. 半透明暗色遮罩：仅在文字可读性不足时添加，透明度约 10%～20%。
3. `logo_emblem.png` 与实时绘制的“回响之刃 / ECHO BLADE”标题。
4. `player_idle.png`：置于城门前方。
5. 多个 `menu_panel.png` 与菜单文字。
6. `menu_cursor.png`：位于当前选项左侧。
7. `parry_spark.png`：作为短暂的最上层特效。
8. 版本号、操作方式等辅助信息：置于屏幕边缘。

标题和菜单不应遮挡中央城门的发光轮廓。默认焦点应落在“开始游戏”或存在存档时的“继续游戏”上。

## 6. Pygame 加载示例

```python
from pathlib import Path

import pygame

IMAGE_DIR = Path(__file__).resolve().parent / "images"

background = pygame.image.load(IMAGE_DIR / "title_background.png").convert()
logo = pygame.image.load(IMAGE_DIR / "logo_emblem.png").convert_alpha()
player = pygame.image.load(IMAGE_DIR / "player_idle.png").convert_alpha()
panel = pygame.image.load(IMAGE_DIR / "menu_panel.png").convert_alpha()
cursor = pygame.image.load(IMAGE_DIR / "menu_cursor.png").convert_alpha()
parry_spark = pygame.image.load(IMAGE_DIR / "parry_spark.png").convert_alpha()
```

背景不含透明通道，使用 `convert()`；其余资源均包含透明通道，应使用 `convert_alpha()`。

推荐先创建 1280×720 的逻辑画布，再统一缩放到实际窗口：

```python
LOGICAL_SIZE = (1280, 720)
canvas = pygame.Surface(LOGICAL_SIZE)

canvas.blit(background, (0, 0))
canvas.blit(logo, (512, 48))
canvas.blit(player, (592, 510))

window_size = window.get_size()
scaled_canvas = pygame.transform.scale(canvas, window_size)
window.blit(scaled_canvas, (0, 0))
```

若需要保持像素比例，应根据窗口尺寸计算整数缩放倍数，并使用黑边填充剩余区域。

## 7. 菜单状态规范

建议通过程序状态控制同一套素材，不必为每种状态重复保存图片。

| 状态 | 面板表现 | 光标 | 文字表现 |
| --- | --- | --- | --- |
| 默认 | 低透明度 | 不显示 | 淡青灰 |
| 悬停/选中 | 完整不透明度 | 显示 | 淡青白 |
| 按下 | 向下偏移 2 像素 | 同步偏移 | 珊瑚红或高亮青 |
| 禁用 | 降低饱和度和透明度 | 不显示 | 深灰色 |

开始界面建议包含以下选项：

- 继续游戏：存在有效存档时显示。
- 开始游戏：创建新一局游戏。
- 排行榜：打开本地排行榜。
- 设置：音量、窗口、键位和辅助功能。
- 退出游戏：弹出确认提示后关闭游戏。

## 8. 动画与反馈建议

- 背景可以加入非常缓慢的星光闪烁，但不移动建筑主体。
- 徽记每 3 秒触发一次轻微亮度变化。
- 菜单切换时播放短促金属音，并让光标快速移动到新位置。
- 确认“开始游戏”时，在屏幕中央播放一次 `parry_spark.png`，随后快速淡出进入游戏。
- 动画应使用经过时间 `dt` 计算，不能依赖固定帧数。
- 设置中应允许降低闪光强度和关闭屏幕震动。

## 9. 后续素材扩展规范

新增图片时遵循以下规则：

1. 文件名使用小写英文和下划线，例如 `button_pressed.png`。
2. 透明素材统一保存为 32 位 PNG，不使用有损 JPG。
3. 像素素材先在低分辨率画布绘制，再按整数倍放大。
4. 相同用途的素材保持一致的画布尺寸、基准线和锚点。
5. 动画帧建议使用序号命名，例如 `parry_spark_00.png`。
6. 不把可本地化文字直接画进图片，文字由程序和字体资源渲染。
7. 每次添加或替换素材时，同步更新本文档的资源清单。

建议后续补充：标题字体、按钮音效、角色待机动画帧、背景粒子、设置图标和排行榜图标。

## 10. 验收检查

- [x] 所有开始界面资源均位于 `images/` 文件夹。
- [x] 背景尺寸为 1280×720，适配 16:9 基准画布。
- [x] 徽记、角色、面板、光标和弹刀特效具备透明通道。
- [x] 图片中没有固定菜单文字，支持后续本地化。
- [x] 图片使用统一像素风格和配色。
- [x] 已提供 Pygame 加载方式、图层顺序和菜单状态说明。
